Here, I have build a clone of Amazon.com in the form of landing page(home page) from scratch.
Technologies used- HTML and CSS.
Concepts that have been used to build the project are semantic HTML elements, images, CSS grid layout, hover effect, icons, CSS flexbox, CSS backgrounds, CSS colors, etc.
It includes sections like- header, navigation, hero banner, product cards, footer, and final polishing (hover effects, spacing).
I have tried to create it like the original Amazon home page till whereever possible.
